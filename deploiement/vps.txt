VPS APS : ssh root@191.218.165.130 / aq)qJ#D6BxS63,aP / apssante

1- REGENERER 
ssh-keygen -t ed25519 -C "danielngono002gmail.com" -f ~/.ssh/id_ed25519_new

1- generer ssh :
 ssh-keygen -t ed25519 -C "danielngono002.com"

2- J'ai deja mes cles ssh donc je vais simplement les copier dans le vps : 

cat ~/.ssh/id_ed25519.pub | ssh root@191.218.165.130 "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys"

3- Tester la connexion sur hPannel > VPS > Browser Terminal 
( ystemctl status ssh ) si erreur > sudo systemctl restart ssh

4- sudo fail2ban-client status sshd ( Verifier si mon IP est bani )
debani : sudo fail2ban-client set sshd unbanip TON_IP
